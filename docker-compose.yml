services:
  app:
    build: .
    container_name: sistema_expedientes
    ports:
      - "${HOST_PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./frontend:/app/frontend:ro
    restart: unless-stopped
    logging:
      driver: "json-file"